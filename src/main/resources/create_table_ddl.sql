DROP TABLE IF EXISTS __schema__.__table__;
CREATE TABLE __schema__.__table__
(
    hash            VARCHAR,
    number          VARCHAR,
    street          VARCHAR,
    unit            VARCHAR,
    city            VARCHAR,
    district        VARCHAR,
    region          VARCHAR,
    postcode        VARCHAR,
    id              VARCHAR
);
